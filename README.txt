Para correr o código é necessário instalar os packages cv2 e progress:
-pip install opencv-python
-pip install progress

Para correr o código:
-python3 rust_detector.py
-Escolher inserir o número da funcionalidade

1:
-Selecionar a pasta (ex: 1)
-Inserir o nome da imagem (ex: 11.jpg)
-Escolher se pretende cortar a imagem (ex: n)
-Clicar numa tecla nas imagens para sair

2:
-Inserir o nome da pasta (ex: No_Corrosion)
-Escolher se pretende inserir um mínimo de percentagem de ferrugem por imagem (ex: y)
-Inserir uma percentagem mínima de ferrugem por imagem (ex: 20)
